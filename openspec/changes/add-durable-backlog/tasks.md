## 1. Durable Backlog Foundation

- [x] 1.1 Add `BacklogItem`, `BacklogKind`, `BacklogStatus`, `BacklogStrength`, `BacklogStatusFilter`, and `BacklogMergeSummary` types in `src/types.ts`.
- [x] 1.2 Add `src/backlog.ts` with: `ensureBacklogSchema(db)`, `openBacklogDatabase(config)`, `deriveStrength(seen, miss)`, `mergeCandidatesIntoBacklog(db, candidates, now)`, `listBacklog(db, status, now)`, `getBacklogItem(db, id)`, `effectiveStatus(item, now)`, `dismissBacklogItem` / `snoozeBacklogItem` / `resolveBacklogItem`. Identity reuses the existing deterministic `ObservationCandidate.id` (already `sourceType-sourceName-reason` slug), no extra hashing.
- [x] 1.3 Add `src/backlog.test.ts` with 17 cases: strength derivation table, schema idempotency, first-cycle insert, recurrence upsert + prev-evidence, miss-count increment, strength drop at miss>=3, auto-stale at miss>=6, dismissed permanence, resolved revive on recurrence, snoozed-but-recurring keeps status, listBacklog active/snoozed/all filters, expired-snooze read-side promotion, snooze input validation, and 404-style null returns.
- [x] 1.4 `openBacklogDatabase` calls `ensureBacklogSchema` on every open, and the observation loop opens it on every cycle, so existing deploys gain the table on first observe without a manual migration step.

## 2. Observation Loop Integration

- [x] 2.1 `src/observation-loop.ts` calls `mergeCandidatesIntoBacklog` after each successful `observe()` and before `getIdeaGraph()`, so the graph step sees backlog-driven node strengths.
- [x] 2.2 `ObservationLoopState` carries `lastBacklogAt` and the loop stamps it on each successful run.
- [x] 2.3 `src/observation-loop.test.ts` covers two consecutive cycles asserting `seenCount=2`, `missCount=0`, `strength='medium'` for the recurring candidate, plus `lastBacklogAt` on state.

## 3. Cockpit Graph Sourcing

- [x] 3.1 `src/idea-graph.ts` loads a backlog lookup at the start of `getIdeaGraph` and `extendIdeaGraphNode`, passes it into `createProjectedGraph`, and the candidate loop now consults the backlog before producing a signal node.
- [x] 3.2 `makeSignalNode` now takes a `BacklogStrength` parameter and uses it as the node's `confidence`; the edge from center → signal also reads the same strength so cockpit CSS visuals match recurrence.
- [x] 3.3 Candidates whose backlog row has effective status `dismissed`, `resolved`, or `snoozed` (with `snoozed_until > now`) are skipped entirely from the graph; they stay reachable via `/api/backlog`.
- [x] 3.4 `src/idea-graph.test.ts` adds three tests: strength=strong propagates to node confidence after 5 cycles, dismissed item produces no signal node, and snoozed item produces no signal node until snooze expires.
- [ ] 3.5 RESEARCH-seed nodes (currently generated by `recurrentKeywords` rather than from candidates) are still derived in-place, not from the backlog. Folded into a follow-up; spec note pending.

## 4. Backlog API And UI

- [x] 4.1 `GET /api/backlog?status=active|snoozed|resolved|dismissed|all` returns `{ items, counts }`; counts reflect effective status (expired snoozes count as active).
- [x] 4.2 `POST /api/backlog/:id/{dismiss,snooze,resolve}` works behind the same trusted-settings check used by graph extension. Snooze validates `days ∈ {1, 7, 30}` and returns 400 otherwise; unknown id returns 404.
- [x] 4.3 Add a visible `Durable Backlog` cockpit panel with filter chips, recent-seen sort copy, current/previous evidence, recurrence metadata, and action buttons. The old per-cycle Observation Workbench remains folded under legacy details for debugging only.
- [x] 4.4 Client-side dismiss / snooze / resolve handlers update the list inline through `/api/backlog` and keep changes metadata-only.
- [x] 4.5 `src/web.test.ts` covers `/api/backlog` GET, snooze/resolve/dismiss POST actions, snooze validation, 404, dashboard rendering, and non-ranking wording.

## 5. Documentation And Release

- [x] 5.1 Bump `package.json`, `package-lock.json`, `src/version.ts`, and `.github/workflows/deploy-dev.yml` `EXPECTED_APP_VERSION` to `0.7.0`.
- [x] 5.2 Add the v0.7.0 entry to `README.md` and `AGENTS.md`.
- [x] 5.3 Capability spec at `specs/durable-backlog/spec.md` is in place (proposal + design also committed).

## 6. Verification And Release

- [x] 6.1 `npm run build`, `npm test`, `docker compose -f docker-compose.kevinhome.yml config`, and `git diff --check` are green for the UI release checkpoint: 59/59 tests.
- [ ] 6.2 Local Docker container smoke is not run on this corporate dev box unless an untracked `Dockerfile.local` MITM workaround is present; use CI-built image plus deploy health checks for the release smoke.
- [ ] 6.3 Commit + push + verify CI / Docker / Deploy for v0.7.0.
